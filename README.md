# PMT - Project Management Tool
PMT is a hybrid between tools like `Trello` and `Azure DevOps`. There are 1-5 columns with cards, and each card has tasks (which are sorted into 3 columns: `To Do`, `In Progress` and `Done`)